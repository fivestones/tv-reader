# Agent Notes

The Android TV web-reader implementation plan lives in
`docs/android-tv-webview-plan.md`. Use that file as the source of truth for the
server, browser UI, preload, and Kotlin WebView wrapper work.
